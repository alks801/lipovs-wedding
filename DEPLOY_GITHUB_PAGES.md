# 🚀 Деплой на GitHub Pages

## Шаг 1: Подготовка проекта

Проект уже настроен для GitHub Pages! Установлены все необходимые зависимости.

### Что было настроено:
- ✅ Установлен пакет `gh-pages`
- ✅ Добавлены скрипты деплоя в `package.json`
- ✅ Настроен `base: './'` в `vite.config.ts` для правильных путей

---

## Шаг 2: Создание репозитория на GitHub

1. Перейдите на [github.com](https://github.com) и войдите в аккаунт
2. Нажмите "New repository" (зелёная кнопка)
3. Заполните:
   - **Repository name**: например, `wedding-invitation` или `lipovs-wedding`
   - **Description**: Приглашение на свадьбу
   - **Public** (если хотите чтобы сайт был доступен всем)
   - ❌ **НЕ** ставьте галочку "Initialize with README"
4. Нажмите "Create repository"

---

## Шаг 3: Подключение локального проекта к GitHub

Откройте терминал в папке проекта и выполните:

```bash
# Инициализация git (если ещё не сделано)
git init

# Добавьте все файлы
git add .

# Первый коммит
git commit -m "Initial commit: Wedding invitation website"

# Подключите удалённый репозиторий (замените USERNAME и REPO_NAME)
git remote add origin https://github.com/USERNAME/REPO_NAME.git

# Отправьте код на GitHub
git branch -M main
git push -u origin main
```

**ВАЖНО:** Замените `USERNAME` на ваш GitHub username, а `REPO_NAME` на название репозитория!

---

## Шаг 4: Обновление package.json

Откройте файл `package.json` и замените строку:

```json
"homepage": "https://<USERNAME>.github.io/<REPO_NAME>",
```

На вашу реальную информацию, например:

```json
"homepage": "https://aleksandr.github.io/wedding-invitation",
```

---

## Шаг 5: Настройка переменных окружения для GitHub Pages

⚠️ **ВАЖНО:** GitHub Pages не поддерживает серверные переменные окружения напрямую.

### Вариант 1: Захардкодить значения (для приватного репозитория)

Создайте файл `src/config.ts`:

```typescript
export const TELEGRAM_BOT_TOKEN = 'ваш_токен_здесь'
export const TELEGRAM_CHAT_ID = 'ваш_chat_id_здесь'
```

И измените `src/utils/telegram.ts`:

```typescript
import { TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID } from '../config'
// Используйте эти константы вместо import.meta.env
```

⚠️ **Внимание:** Этот способ подходит только для **приватных** репозиториев, так как токены будут видны в коде!

### Вариант 2: Использовать Backend для отправки в Telegram (рекомендуется)

Создайте простой backend (например, на Vercel/Netlify Functions), который будет принимать данные формы и отправлять их в Telegram. Так токены останутся в секрете.

---

## Шаг 6: Деплой на GitHub Pages

```bash
# Соберите и задеплойте проект одной командой
npm run deploy
```

Эта команда:
1. Соберёт проект (`npm run build`)
2. Создаст ветку `gh-pages`
3. Загрузит собранные файлы на GitHub

---

## Шаг 7: Включение GitHub Pages

1. Перейдите в настройки репозитория на GitHub
2. В меню слева выберите **Pages**
3. В разделе "Source" выберите:
   - **Branch**: `gh-pages`
   - **Folder**: `/ (root)`
4. Нажмите **Save**
5. Подождите 1-2 минуты

---

## Шаг 8: Проверка сайта

Ваш сайт будет доступен по адресу:

```
https://USERNAME.github.io/REPO_NAME/
```

Например: `https://aleksandr.github.io/wedding-invitation/`

---

## 🔄 Обновление сайта

После любых изменений в коде:

```bash
# Добавьте изменения
git add .

# Создайте коммит
git commit -m "Описание изменений"

# Отправьте на GitHub
git push

# Задеплойте обновлённую версию
npm run deploy
```

---

## 🛠️ Полезные команды

```bash
# Локальная разработка
npm run dev

# Сборка проекта
npm run build

# Предпросмотр собранного проекта
npm run preview

# Деплой на GitHub Pages
npm run deploy
```

---

## ⚠️ Важные замечания

### 1. Файл .env НЕ попадает на GitHub Pages

Файл `.env` добавлен в `.gitignore` и не будет загружен на GitHub. Поэтому:

- **Вариант А**: Захардкодьте токены в `src/config.ts` (только для приватного репозитория)
- **Вариант Б**: Используйте backend API для отправки в Telegram (безопаснее)

### 2. Персонализированные ссылки

Для отправки персонализированных приглашений используйте:

```
https://USERNAME.github.io/REPO_NAME/?name=Иван
```

### 3. Custom Domain (опционально)

Если хотите использовать свой домен:
1. Купите домен (например, на REG.RU)
2. В настройках репозитория GitHub Pages добавьте Custom Domain
3. Настройте DNS записи у регистратора домена

---

## 📝 Checklist перед деплоем

- [ ] Обновлен `package.json` с правильным `homepage`
- [ ] Настроены переменные окружения (Telegram токены)
- [ ] Проверена дата свадьбы (31.07.2026)
- [ ] Заполнен адрес места проведения
- [ ] Заменены placeholder изображения на реальные фото
- [ ] Проверена работа формы локально
- [ ] Создан репозиторий на GitHub
- [ ] Код отправлен на GitHub (`git push`)
- [ ] Запущен деплой (`npm run deploy`)
- [ ] Включен GitHub Pages в настройках репозитория

---

## 🎉 Готово!

После выполнения всех шагов ваш сайт будет доступен по адресу GitHub Pages, и вы сможете делиться ссылками с гостями!

**Пример ссылки для гостя:**
```
https://ваш-username.github.io/название-репо/?name=Иван
```
