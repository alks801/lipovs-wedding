# 🚀 Быстрый деплой - Пошаговая инструкция

## ✅ Что уже готово

Проект полностью подготовлен для деплоя на GitHub Pages!

- [x] Установлен `gh-pages`
- [x] Настроен `vite.config.ts`
- [x] Добавлены скрипты деплоя
- [x] Настроен GitHub Actions workflow

---

## 📋 Шаги для деплоя

### 1. Создайте репозиторий на GitHub

Перейдите на https://github.com/new и создайте новый репозиторий

### 2. Обновите package.json

Откройте `package.json` и замените эту строку:

```json
"homepage": "https://<USERNAME>.github.io/<REPO_NAME>",
```

На ваши данные, например:

```json
"homepage": "https://aleksandr-lipov.github.io/wedding",
```

### 3. Инициализируйте Git и отправьте код

```bash
git init
git add .
git commit -m "Wedding invitation website"
git remote add origin https://github.com/USERNAME/REPO_NAME.git
git branch -M main
git push -u origin main
```

**Замените USERNAME и REPO_NAME на ваши!**

### 4. Настройте Secrets в GitHub

Перейдите в Settings → Secrets and variables → Actions

Добавьте два секрета:
- `VITE_TELEGRAM_BOT_TOKEN` = ваш токен бота
- `VITE_TELEGRAM_CHAT_ID` = ваш chat ID

### 5. Включите GitHub Pages

Settings → Pages → Source: выберите **GitHub Actions**

### 6. Деплой!

Просто сделайте push - GitHub Actions автоматически задеплоит:

```bash
npm run deploy
```

Или просто:

```bash
git push
```

---

## 🌐 Ваш сайт будет доступен по адресу:

```
https://USERNAME.github.io/REPO_NAME/
```

---

## ⚠️ ВАЖНО: Telegram токены

GitHub Pages это статический хостинг, где все видно в коде!

**2 варианта:**

### Вариант 1: Для приватного репозитория
Создайте `src/config.ts` и захардкодьте токены там

### Вариант 2: Безопасный (рекомендуется)
Используйте Netlify Functions - см. `TELEGRAM_BACKEND_SETUP.md`

---

## 🔄 Обновление сайта

После любых изменений:

```bash
git add .
git commit -m "Update"
git push
```

GitHub Actions автоматически пересоберёт и задеплоит!

---

## 📚 Подробные инструкции

- `DEPLOY_GITHUB_PAGES.md` - полная инструкция по деплою
- `TELEGRAM_BACKEND_SETUP.md` - безопасная настройка Telegram через backend

---

## ✅ Checklist

- [ ] Создан репозиторий на GitHub
- [ ] Обновлен `homepage` в `package.json`
- [ ] Код отправлен на GitHub
- [ ] Настроены Secrets (если используете GitHub Actions)
- [ ] Включен GitHub Pages
- [ ] Проверен адрес места проведения
- [ ] Заменены placeholder фото

**Готово! 🎉**
