# 🚀 Быстрый деплой - Пошаговая инструкция

## ✅ Что уже готово

Проект полностью подготовлен для деплоя на GitHub Pages!

- [x] Установлен `gh-pages`
- [x] Настроен `vite.config.ts`
- [x] Добавлены скрипты деплоя
- [x] Настроен GitHub Actions workflow (опционально)

---

## 🎯 Рекомендуемый способ (самый простой)

### 1. Создайте репозиторий на GitHub

Перейдите на https://github.com/new и создайте новый репозиторий

### 2. Обновите package.json

Откройте `package.json` и замените эту строку:

```json
"homepage": "https://<USERNAME>.github.io/<REPO_NAME>",
```

На ваши данные, например:

```json
"homepage": "https://aleksandr-lipov.github.io/wedding",
```

### 3. Отправьте код на GitHub

```bash
git init
git add .
git commit -m "Wedding invitation website"
git remote add origin https://github.com/USERNAME/REPO_NAME.git
git branch -M main
git push -u origin main
```

**Замените USERNAME и REPO_NAME на ваши!**

### 4. Деплой одной командой

```bash
npm run deploy
```

Эта команда автоматически:
- Соберёт проект
- Создаст ветку `gh-pages`
- Загрузит файлы на GitHub

### 5. Настройте GitHub Pages

1. Откройте Settings → Pages
2. В **Source** выберите:
   - **Deploy from a branch**
   - Branch: `gh-pages`
   - Folder: `/ (root)`
3. Нажмите **Save**

### 6. Готово! 🎉

Через 1-2 минуты сайт будет доступен по адресу:
```
https://USERNAME.github.io/REPO_NAME/
```

---

## 🔄 Обновление сайта

После любых изменений:

```bash
git add .
git commit -m "Update"
git push
npm run deploy
```

---

## 🤖 Альтернатива: GitHub Actions (автоматический деплой)

Если хотите автоматический деплой при каждом push:

### 1. Включите GitHub Pages

Settings → Pages → Source: выберите **GitHub Actions**

### 2. Push код

```bash
git push
```

Workflow автоматически запустится и задеплоит сайт!

⚠️ **Если получаете ошибку:** см. файл `TROUBLESHOOTING.md`

---

## ⚠️ ВАЖНО: Telegram токены

GitHub Pages это статический хостинг, где всё видно в коде!

**2 варианта:**

### Вариант 1: Для приватного репозитория
Создайте `src/config.ts` и захардкодьте токены там

### Вариант 2: Безопасный (рекомендуется)
Используйте Netlify Functions - см. `TELEGRAM_BACKEND_SETUP.md`

---

## ✅ Checklist

- [ ] Создан репозиторий на GitHub
- [ ] Обновлен `homepage` в `package.json`
- [ ] Код отправлен на GitHub (`git push`)
- [ ] Выполнен деплой (`npm run deploy`)
- [ ] Настроен GitHub Pages (Source: gh-pages branch)
- [ ] Проверен адрес места проведения
- [ ] Заменены placeholder фото
- [ ] Настроен Telegram (безопасно!)

---

## 🆘 Проблемы?

См. файл **`TROUBLESHOOTING.md`** с решениями частых проблем!

---

## 📚 Подробные инструкции

- `TROUBLESHOOTING.md` - решение проблем ⚠️
- `DEPLOY_GITHUB_PAGES.md` - полная инструкция по деплою
- `TELEGRAM_BACKEND_SETUP.md` - безопасная настройка Telegram

---

**Готово! 🎉**
