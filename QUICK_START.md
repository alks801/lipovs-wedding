# 🎊 Quick Start Guide

## Первые шаги

### 1. Настройте Telegram бота

Для получения ответов от гостей вам нужен Telegram бот:

**Создание бота:**
1. Откройте Telegram и найдите @BotFather
2. Отправьте команду: `/newbot`
3. Следуйте инструкциям (выберите имя и username)
4. **Сохраните токен**, который выдаст BotFather (выглядит как: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

**Получение Chat ID:**
1. Напишите вашему боту любое сообщение в Telegram
2. Откройте в браузере: `https://api.telegram.org/bot<ВАШ_ТОКЕН>/getUpdates`
   (замените `<ВАШ_ТОКЕН>` на токен из предыдущего шага)
3. Найдите строку `"chat":{"id":123456789}`
4. **Сохраните это число** - это ваш Chat ID

### 2. Создайте файл .env

Создайте файл `.env` в корне проекта и добавьте:

```
VITE_TELEGRAM_BOT_TOKEN=ваш_токен_от_BotFather
VITE_TELEGRAM_CHAT_ID=ваш_chat_id
```

### 3. Запустите проект

```bash
npm run dev
```

Откройте браузер на `http://localhost:5173`

## 📋 Что нужно изменить перед свадьбой

### В файле `src/components/MainInvitation.tsx`:

1. **Имена молодоженов** (строка 23):
   ```jsx
   <h1 className="couple-names">
     Ваше Имя & Имя Партнера
   </h1>
   ```

2. **Дата свадьбы** (строки 31-36):
   ```jsx
   <div className="date-number">15</div>
   <span className="month">августа</span>
   <span className="year">2026</span>
   ```

3. **Время** (строка 45):
   ```jsx
   <p>16:00</p>
   ```

4. **Место проведения** (строки 51-52):
   ```jsx
   <h3>Место</h3>
   <p>Ресторан "Название"<br />ул. Адрес, 123</p>
   ```

5. **Дресс-код** (строки 57-58):
   ```jsx
   <h3>Дресс-код</h3>
   <p>Коктейльный стиль</p>
   ```

### В файле `src/components/Footer.tsx`:

Измените имена (строка 7):
```jsx
<p className="footer-text">С любовью, Ваши Имена</p>
```

## 🖼️ Замена фотографий

Замените placeholder изображение:
- `public/images/couple-1.jpg` - ваша фотография

Рекомендуемый размер: 800x600px или больше

## 🎨 Изменение цветов

В файле `src/styles/main.css` (строки 9-14):

```css
:root {
  --cream: #FAF7F0;      /* Фон светлый */
  --blush: #E8C4C4;      /* Розовый акцент */
  --sage: #A8B5A0;       /* Зеленый светлый */
  --dark-sage: #7D8F7A;  /* Зеленый темный */
  --accent: #D4A5A5;     /* Дополнительный акцент */
}
```

## 🔗 Отправка приглашений гостям

### Общая ссылка:
```
https://ваш-сайт.com/
```

### Персонализированная ссылка:
```
https://ваш-сайт.com/?name=Иван
```

Каждому гостю можно отправить уникальную ссылку с его именем!

## 📱 Тестирование

1. Откройте главную страницу
2. Перейдите на страницу `/survey`
3. Заполните форму
4. После отправки проверьте, что сообщение пришло в Telegram

## 🚀 Деплой на Vercel (бесплатно)

1. Зарегистрируйтесь на [vercel.com](https://vercel.com)
2. Установите Vercel CLI: `npm i -g vercel`
3. В корне проекта запустите: `vercel`
4. Следуйте инструкциям
5. В настройках проекта на Vercel добавьте переменные окружения:
   - `VITE_TELEGRAM_BOT_TOKEN`
   - `VITE_TELEGRAM_CHAT_ID`

## ❓ Частые вопросы

**Q: Не приходят сообщения в Telegram**
A: Проверьте:
- Токен бота правильный
- Chat ID правильный
- Вы написали боту хотя бы одно сообщение
- Файл `.env` находится в корне проекта

**Q: Как добавить больше полей в форму?**
A: Отредактируйте `src/components/Survey.tsx`, добавьте новые поля в интерфейс `FormData` и в JSX форму

**Q: Можно ли сделать сайт на другом языке?**
A: Да! Просто замените весь текст в компонентах на нужный язык

**Q: Как изменить шрифты?**
A: Измените импорт Google Fonts в `index.html` и переменные в `main.css`

---

Удачи с организацией свадьбы! 💐
